# UI Change Plan Template

# <Observable outcome>

Based on revision: `<commit, timestamp, or unavailable>`

## Why this change exists

- Affected surface: `<route, state, or component path>`
- Observation: `<what the user currently experiences>`
- Governing evidence: `<source and exact decision>`
- Runtime connection: `<how the owner reaches this surface>`
- Root correction: `<one determined change>`

## Ownership and reuse

- Existing owner to change: `<token, primitive, variant, composition, or copy source>`
- Consumers that inherit the change: `<paths or surfaces>`
- Existing exemplar: `<path, or none>`

If a new owner is necessary, explain why no existing contract can express the change and which consumers will share it.

## Implementation

1. `<exact file or surface>`
   - Change: `<implementation-ready instruction>`
   - Preserve: `<behavior, identity, or exception>`
   - Observe: `<visible result>`

## Scope boundaries

- Include: `<affected consumers>`
- Inspect for regressions: `<adjacent consumers>`
- Exclude: `<unrelated or valid exceptions>`

## Validation

- User journey: `<action and expected outcome>`
- Interface states: `<viewports, states, content extremes, and interaction modes>`
- System integrity: `<reuse and absence of parallel patterns>`
- Repository checks: `<exact commands and expected results>`

## Stop conditions

- Stop and reassess if `<ownership, evidence, compatibility, or scope assumption>` is false.

## Documentation follow-up

- After acceptance: `<decision and destination, or none>`
